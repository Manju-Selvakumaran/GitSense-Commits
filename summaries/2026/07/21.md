# Activity Summary for 7/21/2026

## 5:09:47 PM
The `app.py` file, last modified on 7/21/2026 at 3:32:28 PM, serves as the main Streamlit frontend for the DeepMuSyC drug synergy prediction application. It handles user interaction, allowing selection of two drugs and a cell line for evaluation. The application features a "Developer mode" toggle which, if enabled and ground-truth data is available, overlays NCI-ALMANAC data for comparison. Key functionalities include fetching drug and cell line data (with caching for performance), calling a `drug_inference` module to predict 9 MuSyC parameters, and rendering the results. Errors related to data loading or PubChem/RDKit lookups are handled. The application distinguishes between production and developer rendering, with the latter showing both predicted and ground-truth dose-response surfaces, along with a table comparing their MuSyC parameters. A warning is displayed if running in "placeholder mode" without a trained model checkpoint.

The `frontend\synergy.py` file, observed twice with identical content at 7/21/2026, 3:34:04 PM and 7/21/2026, 3:34:17 PM, is responsible for the mathematical calculation and 3D visualization of the MuSyC dose-response surface. It defines constants (E0, R1, R2) and explicitly lists the 9 MuSyC parameters (`PARAM_KEYS`). The core logic resides in `_edrug2d_ndb`, a NumPy port of a complex log-space MuSyC NDB dose-response function, which computes the drug synergy surface. The `musyc_surface` function evaluates this model over a dynamically generated dose grid centered on the drug's potency. The `build_figure` function then uses Plotly to construct a dark-themed 3D surface plot, styled to match an existing HTML explorer. It supports custom colorscales and shared z-axis ranges, crucial for comparing multiple surfaces (e.g., predicted vs. ground truth). Additionally, it can overlay raw lab measurements as 3D scatter points, excluding zero-dose controls due to the log-scaled axes.

A recurring pattern across these files is the central role of the 9 MuSyC parameters, which are consistently referenced for both prediction and visualization. Both files also demonstrate a clear focus on the 3D dose-response surface, with `synergy.py` handling its numerical generation and `app.py` orchestrating its display and comparative analysis within the user interface, especially evident in the "Developer mode." The design emphasizes robust mathematical computations in `synergy.py`, including numerical stability considerations with `_safe_log` and extensive log-space calculations, to accurately model the drug interactions.